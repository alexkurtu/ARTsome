<div class="container">
<!--  <div style="width: 100%; height: 600px;"-->
<!--       data-controller="map"-->
<!--       data-map-markers-value="<%#= @markers.to_json %>"-->
<!--       data-map-api-key-value="<%#= ENV['MAPBOX_API_KEY'] %>">-->
<!--  </div>-->
  <div class="gallery">
    <% @arts.each do |item| %>
      <div class="gallery-item img-h">
        <a href="<%= art_path(item) %>">
            <%= cl_image_tag item.photo.key, height: 300, crop: :fill if !item.photo.key.nil? %>
        </a>
      </div>
    <% end %>
  </div>
</div>
